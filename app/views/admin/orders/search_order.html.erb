<!DOCTYPE html>
<html>
  <head>
    <style> 
input[type=text] {
  width: 130px;
  box-sizing: border-box;
  border: 2px solid #ccc;
  border-radius: 4px;
  font-size: 16px;
  background-color: white;
  background-image: url('searchicon.png');
  background-position: 10px 10px; 
  background-repeat: no-repeat;
  padding: 12px 20px 12px 40px;
  -webkit-transition: width 0.4s ease-in-out;
  transition: width 0.4s ease-in-out;
}

input[type=text]:focus {
  width: 100%;
}
</style>

  </head>
  <!-- Coded with love by Mutiullah Samim-->
  <body>
    <div class="container h-100" style="margin-top: 80px; margin-bottom: 40px;">
      <h1>Search by Order's ID</h1>
      <div class="d-flex justify-content-center h-100">
          <div class="container" style="margin-top: 20px; margin-bottom: 40px;">
        <%= form_tag(admin_showorder_path, class: "searchbar", method: :get) do %>
          <%= text_field_tag(:id, params[:id], placeholder: "enter order's number") %>
          <%= submit_tag 'Search' %>
        <% end %>
      </div>
      </div>

      <h1>Search by Order's Date</h1>
      <div class="d-flex justify-content-center h-100">
          <div class="container" style="margin-top: 20px; margin-bottom: 40px;">
        <%= form_tag(admin_showorder_path, class: "searchbar", method: :get) do %>  
        <%= select_date Date.today, prefix: :start_date %>
        <%= submit_tag 'Search' %>
        <% end %>
      </div>
      </div>
    </div>
  </body>
</html>



