<!DOCTYPE html>
<html lang="en">
<head>
  <title>Bootstrap Example</title>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.0/js/bootstrap.min.js"></script>
</head>
<body>

<div class="container" style="margin-top: 140px; margin-bottom: 30px;">
  <% if @user %>
  <h2>Orders for @user.name </h2>
  <% else %>
  <h2>Orders</h2>            
  <% end %>
  <table class="table table-striped">
    <thead>
      <tr>
        <th>Orders</th>
        <th>Date Created</th>
        <th>Total</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>John</td>
        <td>Doe</td>
        <td>john@example.com</td>
      </tr>
    </tbody>
  </table>
</div>
   <div class="container h-100" style="margin-top: 10px; margin-bottom: 40px;">
      <div class="d-flex justify-content-center h-100">
          <div class="container">
        <%= form_tag(admin_orders_path, class: "searchbar", method: :get) do %>
          <%= text_field_tag(:id, params[:id], placeholder: "Search by user's id number.") %>
          <%= submit_tag 'Search' %>
        <% end %>
      </div>
      </div>
    </div>
</body>
</html>
